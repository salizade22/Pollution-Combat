public enum Character {
    //this is to define our characters and categorize them into certain subsets of GameObjects
    TANK,
    BULLET,
    AEROPLANE,
    SHIP,
    SUBMARINE,
    RAIN,
    TOXIN,
    ALIENSHIP,
    CLOUD;
}
